{
  "name": "records-docker-server",
  "version": "1.0.0",
  "description": "Node.js server for Docker container management",
  "main": "docker-server.js",
  "scripts": {
    "start": "node docker-server.js",
    "dev": "nodemon docker-server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
